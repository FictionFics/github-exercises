name: "Motivation Bot"

on:
  issues:
    types: [labeled]

jobs:
  motivate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Motivate Action
        uses: ./.github/actions/motivate-actions
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
